import { servidor } from "../server/client-server.js";

let verContatos = document.querySelector(".verContatos")


/* verContatos.addEventListener('click', async () => {
    let Key = localStorage.getItem("authKey")
    let responseTeste = await servidor.viewContacts(Key)
    console.log(responseTeste)
}
) */